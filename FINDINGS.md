# Findings

第 1 批领域实现过程中记录的 API 摩擦：

- ~~`gor.Binder` 没有暴露注入的时钟，`gor.Runtime` 也没有公开读取时钟的方法。为了不在示例生产代码里使用 `time.Now()`，设备工厂必须额外接收 `clock.Clock`。~~ **已解决（第 5.5 步：新增 `gor.Now(b)`）。**
- ~~`gor.Binder` 没有提供跨实体引用入口。设备要通知车间，只能让注册闭包捕获 `*gor.Runtime`，再调用 `gor.Ref[Workshop]`；实体构造因此和运行时对象发生了额外耦合。~~ **已解决（第 5.5 步：新增 `gor.Ref[T](b, key)`）。**
- 设备影子写入、掉线闹钟重置和车间通知是三个独立操作，没有事务或 outbox。示例会把每个错误返回给调用方，但如果影子已经写入而后续闹钟或通知失败，设备和车间可能暂时不一致，调用方也没有一个统一的重试状态可查询。
- ~~定时器轮询器在投递定时方法时丢弃了调用错误。因而 `MarkOffline` 触发的车间通知失败不会被示例的调用方观察到，也不会自动重试；这不是示例可以在不改库的情况下修复的行为。~~ **已解决（第 5.5 步：新增 `gor.OnError` 错误出口）。**
- `gor.State[T].Get()` 返回激活中持有的那一份值。对 map 原地修改后仍必须调用 `Set()`，写成功时内存和存储才一致；写失败时激活会被停用并从存储重新读取。这个共享值语义在文档里没有说清楚，示例最初因此多做了复制和 nil 防护。
- ~~`docs/programming-model.md` 和 `design/runtime.md` 的状态机图都承诺了 `OnActivate` / `OnDeactivate`，但当前代码里这两个钩子一行都没有。示例无法观察实体何时激活或何时因闲置被驱逐；负载程序只验证闲置超时后状态仍可读，不把这误称为生命周期证明。这是文档承诺未实装，已进入 ROADMAP 第 5.5 步。~~ **已解决（第 5.5 步：实现生命周期钩子，并让负载示例观察闲置驱逐）。**
